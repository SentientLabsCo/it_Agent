/*
This is a Flutter widget for the notification screen.
It is designed to display notifications related to the IT support application.
Currently, it is a placeholder and does not implement any specific functionality.
Author: [Arpit Raghuvanshi]
*/

import 'package:flutter/material.dart';

class NotificationScreen extends StatelessWidget {
  
  const NotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Notifications",
        style: TextStyle(fontSize: 28, color: Colors.white),
      ),
    );
  }
}
